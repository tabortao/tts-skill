# 2026-01-28 进度条与英文文档

## 目标

- `python tts-skill.py qwen3-tts ...` 执行时实时显示进度条与预计完成时间
- 任务结束后输出总运行时间、每个汉字（或字符）平均耗时
- 所有 Python 引擎默认输出到 `output/`，默认文件名为 `YYYYMMDD_HHMMSS_文本前6字.*`
- 提供英文版 `INSTALL.md`、`README.md`、`SKILL.md`，同时保留中文版备查

## 实施步骤

- [x] 调整 qwen3-tts-cli：运行临时脚本时不再吞掉输出，确保进度实时刷新
- [x] 调整 tts-skill：为 qwen3-tts 增加计时与“每汉字耗时”统计输出
- [x] 统一默认输出与命名规则（tts-skill / edge-tts-cli / qwen3-tts-cli / openai-tts-cli）
- [x] 产出英文文档并保留中文副本（*.zh-CN.md）
- [x] 用示例命令在 Windows 下做验证

## 验收点

- 运行 qwen3-tts 时能看到持续更新的进度与预计剩余时间
- 结束后输出：总运行时间、汉字数、平均每汉字耗时（无汉字则用字符数）
- 不传 `--output` 时默认写入 `output/` 且文件名符合规则
- 根目录三份文档为英文，且存在对应中文副本
